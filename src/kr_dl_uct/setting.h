#include <math.h>

const int action_dim = 3;
const int score_dim = 17;

const double std_x = 0.145 * 0.5;
const double std_y = 0.145 * 2.0;

const double var_x = pow((0.145 * 0.5), 2.0);
const double var_y = pow((0.145 * 2.0), 2.0);